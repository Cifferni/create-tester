{
  "name": "@create-tester/core",
  "version": "0.9.2",
  "description": "create-tester 引擎内核:接口断言 API / 用例解析 / 页面快照 / MCP 工具服务器(测试脚手架共用内核)",
  "license": "MIT",
  "engines": {
    "node": ">=20.6"
  },
  "bin": {
    "create-tester-mcp": "dist/mcp/server.cjs"
  },
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.cjs",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.2",
    "jiti": "^2.7.0",
    "playwright": "^1.62.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz",
    "zod": "^3.25 || ^4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
